apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: bg-demo
spec:
  type: NodePort
  selector:
    app: example-app
    env: green   # 👈 aquí empiezas apuntando a GREEN
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
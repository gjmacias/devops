apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-green
  namespace: bg-demo
spec:
  replicas: 3
  selector:
    matchLabels:
      app: example-app
      env: green
  template:
    metadata:
      labels:
        app: example-app
        env: green
    spec:
      containers:
      - name: app
        image: demoapp:v2.0
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "250m"
            memory: "128Mi"
          requests:
            cpu: "100m"
            memory: "64Mi"